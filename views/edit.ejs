<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <h2 class="mb-5">you can edit a post.</h2>
        <form action="/update/<%= post._id %>" method="post">
            <textarea
              name="content"
              class="p-3 block mb-2 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"
            ><%= post.content %></textarea>
            <input type="submit" class="px-4 py-2 rounded-full font-medium block bg-yellow-500 text-black w-20" value="post"/>
          </form>
          
      
    </div>
  </body>
</html>
